# Mobile (Unity AR Application)

This directory contains the Unity project for the AR-based Safety Training Simulator.

## Notes
- Only the `Assets`, `Packages`, and `ProjectSettings` folders of the Unity project should be committed.
- Ensure you have the required Unity version and AR packages installed.
